export function createAssignment(service, input, actorId) {
  return service.createAssignment(input, actorId);
}

export function submitAssignment(service, input, actorId) {
  return service.submitAssignment(input, actorId);
}
